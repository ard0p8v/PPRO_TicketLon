<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <intercept-url pattern="/admin/**" access="hasAnyRole('ROLE_ADMIN')" />
        <intercept-url pattern="/manager/**" access="hasAnyRole('ROLE_ADMIN','ROLE_MANAGER')" />
        <intercept-url pattern="/project**" access="isAuthenticated()"/>
        <intercept-url pattern="/comments**" access="isAuthenticated()"/>
        <intercept-url pattern="/user**" access="isAuthenticated()"/>
        <intercept-url pattern="/task**/**" access="isAuthenticated()"/>
        <intercept-url pattern="/dashboard**" access="isAuthenticated()"/>
        <intercept-url pattern="/task**/remove/**" access="hasAnyRole('ROLE_ADMIN','ROLE_MANAGER')" />
        <form-login login-page="/login" default-target-url="/events" authentication-failure-url="/login?error" username-parameter="username" password-parameter="password"/>
        <logout logout-success-url="/login?logout" />
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider user-service-ref="userDetailsServiceImpl">
            <password-encoder ref="encoder"></password-encoder>
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="userDetailsServiceImpl" class="cz.uhk.fim.ppro.service.impl.UserDetailsServiceImpl"></beans:bean>

    <beans:bean id="encoder" name="bCryptPasswordEncoder"
                class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <beans:constructor-arg name="strength" value="11"/>
    </beans:bean>
</beans:beans>